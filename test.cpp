#include "test.h"
#include <stdlib.h>
#include <stdio.h>
#define LENGTH 16
void Test::sayHello(const char *s){
    puts(s);
}
void Test::sayWhat(const char *s){
    puts(s);
}

const char* Test::foo(const char *input){
   // char* m = malloc(LENGTH*sizeof(char));
   
   const  char* s1="abc";
  
   // printf("size of buf1: %d\n", sizeof(buf1));
    return s1;

}


